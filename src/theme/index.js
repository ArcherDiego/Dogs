const container = {
    maxWidth: '80%',
    padding: '0px 1rem',
    margin: '0 auto',
}

const topContainer = {
    marginTop: '4rem',
}

const fontFamily = {
    first: 'Helvetica, Arial, sans-serif',
    secund: 'Spectral, Georgia',
}

const theme = {
    topContainer,
    container,
    fontFamily,
}

export default theme
