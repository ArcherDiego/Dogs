const container = {
    maxWidth: '50rem',
    padding: '0px 1rem',
    margin: '0 auto',
}

const fontFamily = {
    first: 'Helvetica, Arial, sans-serif',
    secund: 'Spectral, Georgia',
}

const theme = {
    container,
    fontFamily,
}

export default theme
