import styled from "styled-components";

export const ListCommentsStyle = styled.ul`
    padding: 0 2rem;
    overflow-y: auto;
    word-break: break-word;

    li{
        margin-bottom: .5rem;
        line-height: 1.2;
    }
`;

export const SingleListCommentsStyle = styled.ul`
    padding: 0;
    overflow-y: auto;
    word-break: break-word;

    li{
        margin-bottom: .5rem;
        line-height: 1.2;
    }
`;
